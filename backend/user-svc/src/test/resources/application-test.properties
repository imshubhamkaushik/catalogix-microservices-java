spring.application.name=user-svc
server.port=11001

spring.datasource.url=${SPRING_DATASOURCE_URL:}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}

# ---------------------------------------------------------------------------
# This profile is for slice tests (@WebMvcTest, mock-based unit tests) that
# don't want a real database at all — DataSource/JPA/Flyway are excluded
# below specifically so those tests stay fast and DB-free. It does NOT set
# up a real Postgres connection, despite an earlier version of this comment
# claiming tests ran against "a real Testcontainers PostgreSQL instance" —
# nothing in this repo used Testcontainers until AuthFlowIntegrationTest
# was added (src/test/java/.../integration/), which needs the real thing
# and overrides these exclusions itself via @DynamicPropertySource rather
# than relying on this shared file. Flyway migrations already exist
# (src/main/resources/db/migration/V1-V5) — the old "once migrations are
# added" framing here was stale.
# ---------------------------------------------------------------------------
spring.jpa.hibernate.ddl-auto=none
spring.datasource.initialize=false
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

spring.sql.init.mode=never

management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoints.web.base-path=/actuator
# In Spring Boot 3, endpoints are usually enabled by default. The new access property controls permission levels. read_only is the standard replacement for "enabled".
management.endpoint.prometheus.access=read_only
management.endpoint.health.show-details=always

# The configuration hierarchy was reorganized to group properties by the monitoring system (Prometheus) rather than the function (metrics.export).
management.prometheus.metrics.export.enabled=true
management.server.port=11001
management.health.db.enabled=false

# Enable histogram buckets
management.metrics.distribution.percentiles-histogram.http.server.requests=true

# Explicitly enable distribution statistics
management.metrics.distribution.slo.http.server.requests=50ms,100ms,200ms,300ms,500ms,1s,2s,5s

management.metrics.distribution.minimum-expected-value.http.server.requests=10ms
management.metrics.distribution.maximum-expected-value.http.server.requests=10s

spring.autoconfigure.exclude=\
org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\
org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration