Catalogix deployed to namespace: {{ .Release.Namespace }}

Backend services ({{ len .Values.backendServices }} total):
{{- range $name, $svc := .Values.backendServices }}
  - {{ $name }} :{{ $svc.port }}{{ if $svc.routeToALB }} (routed: {{ $svc.ingressPath }}){{ else }} (internal-only, no ALB route){{ end }}
{{- end }}
  - frontend :{{ .Values.frontend.service.port }} (routed: {{ .Values.ingress.paths.frontend }})

Check rollout status:
  kubectl get pods -n {{ .Release.Namespace }}
  kubectl get deployment -n {{ .Release.Namespace }}

Get the ALB hostname:
  kubectl get ingress catalogix-gateway-ingress -n {{ .Release.Namespace }} \
    -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'

RabbitMQ management UI (port-forward, don't expose publicly):
  kubectl port-forward svc/rabbitmq 15672:15672 -n {{ .Release.Namespace }}
  then open http://localhost:15672
